CREATE TABLE EMPLOYEE(
	EMPID VARCHAR(10) PRIMARY KEY,
    EMPNAME VARCHAR(20),
    EMP_AGE INT UNSIGNED,
    EMP_SALARY DECIMAL(10,2),
    EMP_DEPARTMENT VARCHAR(10)
);
DESCRIBE EMPLOYEE;
ALTER TABLE EMPLOYEE MODIFY EMP_DEPARTMENT VARCHAR(50);
DESCRIBE EMPLOYEE;
